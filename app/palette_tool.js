pixel_studio.palette_tool = {
	init : function(arg){

		let 	$tools 	= $('#palette-tool'),
			$one 		= $tools.children('a').children('i').detach();
			console.log($one)
		for (var i = 0; i < arg.length; i++) {
			// let  t = 
		}
	}
}